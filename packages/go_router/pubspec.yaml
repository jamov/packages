name: go_router
description: go_router but allows matching multiple routes
version: 3.1.0
repository: https://github.com/jamov/packages/tree/main/packages/go_router

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=2.0.0"

dependencies:
  collection: ^1.15.0
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  logging: ^1.0.0
  meta: ^1.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
